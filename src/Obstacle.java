import processing.core.PImage;

public class Obstacle extends Entity {
    public Obstacle() {
        count++;
    }
    public Obstacle (int row, int col, PImage image) {
        count++;
        super(row, col, )
    }
}
